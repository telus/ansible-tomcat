---
- name: Install Packages | apt
  apt:
    state: latest
    pkg: "{{ item }}"
  with_items:
    - tomcat7
  tags:
    - tomcat
    - software-installation
    - using-apt

- name: Start Service | tomcat7
  service:
    name: tomcat7
    state: started
    enabled: yes
  tags:
    - tomcat
